## 14:19 工作记录

### 完成事项

- 修复 Lima Provider 对 `socket_vmnet` 的所有权冲突：VM 保留 `user-v2` 网络，并通过 `/var/run/socket_vmnet` 连接现有 unmanaged 守护进程，不再重复启动 managed `lima:shared`。
- 增加 VM 网络表达式回归测试，约束 unmanaged socket 配置并防止重新引入 `lima:shared`。
- 清理被 Colima 覆盖的 Ohmykube Lima 网络配置和失败集群状态，重新安装 CLI 并完成真实集群重建。
- 新增 BUG-0002 记录，沉淀 managed/unmanaged 网络模式混用的根因、修复和验证证据。
- 完成 `go test ./...`、`go vet ./...`、Provider race test；真实环境中 3 台 Lima VM 均为 Running，3 个 Kubernetes Node 与全部系统 Pod 均为 Ready，API `/readyz` 和 `/livez` 均返回 `ok`。

### 关联 Commit

- `fix(provider): use unmanaged socket_vmnet network (BUG-0002)`

### 备注

主机侧已移除 `~/.zshrc` 中的全局 `LIMA_HOME`，避免 Colima 再次覆盖 Ohmykube 专用 Lima 配置；该用户配置不属于仓库提交范围。
